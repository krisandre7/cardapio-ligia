#!/bin/bash

# Criar imagem
docker build -t cardapio-ligia-backend .

# --tag , -t		Name and optionally a tag in the name:tag format

# Fonte: https://docs.docker.com/engine/reference/commandline/build/